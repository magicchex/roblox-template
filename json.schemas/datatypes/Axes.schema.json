{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "array",
    "title": "Axes",
    "description": "Locks the object to move in a single axis.",
    "uniqueItems": true,
    "minItems": 1,
    "maxItems": 3,
    "items": {
        "enum": ["X", "Y", "Z"]
    }
}
